{
  "name": "drip-cloud-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute drip-cloud --file=./schema.sql",
    "db:local": "wrangler d1 execute drip-cloud --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.13.1"
  }
}
